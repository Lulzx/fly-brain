node_modules
data/raw
data/skeletons
.venv
dist
body/flybody-src
data/*.npy
data/calib_log.jsonl

scripts/_tmp
data/ir
__pycache__

# Regenerated in ~30 s by scripts/synapse_confidence.py (31 MB of derived arrays;
# the results themselves are in the tracked public/data/edge_uncertainty.json)
public/data/edge_reliability.u8
public/data/edge_w_shrunk.u16

# S2 fit data: 1.4 GB of recorded boundary spike trains, regenerated by
# scripts/record_vnc_fitdata.mjs (descriptor json is tracked)
public/data/vnc_fitdata.bin

# Regenerated by scripts/prep_flysuite_dump.py (34 MB; no current consumer -- kept
# out of the tree until the inverse-dynamics path lands)
public/data/flysuite_frames.bin

# Bend native builds of malecns.bend (bend malecns.bend -o malecns)
malecns
malecns.gpu
cordcheck
cordcheck.gpu
cordrun
cordrun.gpu
